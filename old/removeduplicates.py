import os
stops_file = open("google_transit/stop_times.txt", "r+")

f = open('stop_times_formated.txt', 'w')

num_lines = 0
for line in stops_file:
	num_lines += 1

for i in range(1, num_lines, )

	# new_array = []
	# length = len(array)
	# for i in range(1, length, 3):
	# 	new_array.append(array[i])
	# return new_array